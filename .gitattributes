# Normalize to LF in the repository. Terraform, YAML, and JSON are consumed by
# Linux tooling in-cluster; PowerShell is checked out CRLF on Windows.
* text=auto eol=lf
*.ps1 text eol=crlf
*.json text eol=lf
*.tf   text eol=lf
*.yaml text eol=lf
